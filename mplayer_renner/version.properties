# MPlayer (Renner) — single source of truth for versioning.
# Managed automatically by build_release.ps1 / build.sh (Semantic Versioning).
# VERSION_CODE must always increase and never repeat.
VERSION_NAME=1.0.0
VERSION_CODE=100
