<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <permission
        android:name="com.mupa.player.enterprise.permission.SET_DEV_MODE"
        android:protectionLevel="signature" />

    <!-- normal (não signature): Argos é um app/keystore separado do MPlayer, não pode usar
         permissão signature-level. O Argos já é Device Owner com controle total do aparelho,
         então confiar num broadcast dele pra abrir uma tela de configurações é consistente
         com o nível de confiança que ele já tem. -->
    <permission
        android:name="com.mupa.player.enterprise.permission.OPEN_SETTINGS"
        android:protectionLevel="normal" />

    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />

    <queries>
        <package android:name="com.mupa.agent.argos" />
        <provider android:authorities="com.mupa.agent.argos.provider.device" />
    </queries>

    <application
        android:name=".MupaApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_mplayer"
        android:roundIcon="@drawable/ic_mplayer"
        android:label="@string/app_name"
        android:theme="@style/Theme.MupaPlayerEnterprise"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:screenOrientation="fullUser"
            android:theme="@style/Theme.MupaPlayerEnterprise.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.PlayerActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="fullUser"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.MupaPlayerEnterprise" />

        <activity
            android:name=".ui.DeviceRegistrationActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="fullUser"
            android:theme="@style/Theme.MupaPlayerEnterprise" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="fullUser"
            android:theme="@style/Theme.MupaPlayerEnterprise" />

        <activity
            android:name=".ui.FaceRecognitionTestActivity"
            android:exported="true"
            android:screenOrientation="fullUser"
            android:theme="@style/Theme.MupaPlayerEnterprise" />

        <receiver
            android:name=".ui.DevModeReceiver"
            android:exported="true"
            android:permission="com.mupa.player.enterprise.permission.SET_DEV_MODE">
            <intent-filter>
                <action android:name="com.mupa.player.enterprise.ACTION_SET_DEV_MODE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ui.OpenSettingsReceiver"
            android:exported="true"
            android:permission="com.mupa.player.enterprise.permission.OPEN_SETTINGS">
            <intent-filter>
                <action android:name="com.mupa.player.enterprise.ACTION_OPEN_SETTINGS" />
            </intent-filter>
        </receiver>

        <service
            android:name=".services.InactivityTimerService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
